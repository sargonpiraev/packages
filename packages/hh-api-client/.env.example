# HeadHunter API Configuration

# Optional: Access token for authenticated requests
# Get this from HeadHunter OAuth flow
HH_ACCESS_TOKEN=your_access_token_here

# Required: User-Agent for API requests
# Format: "AppName/Version (contact@example.com)"
HH_USER_AGENT="MyApp/1.0.0 (contact@example.com)"

# Optional: Custom API base URL (defaults to https://api.hh.ru)
HH_API_BASE_URL=https://api.hh.ru

# Optional: Request timeout in milliseconds (defaults to 30000)
HH_TIMEOUT=30000
