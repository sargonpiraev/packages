{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Product pulumi.harness.json",
  "description": "Optional when pulumi/ exists. Declares TypeScript entry relative to pulumi/. Presence of pulumi/ also requires Pulumi.yaml, the entry file, and root package.json scripts pulumi:preview + pulumi:up (ESLint rule). Skip entirely when the project has no pulumi/. Shipped by @sargonpiraev/eslint-config.",
  "type": "object",
  "required": ["entry"],
  "properties": {
    "$schema": { "type": "string", "minLength": 1 },
    "entry": {
      "type": "string",
      "description": "TS entry under pulumi/, e.g. index.ts or src/index.ts",
      "pattern": "^(index\\.ts|src/index\\.ts)$"
    }
  },
  "additionalProperties": false
}
