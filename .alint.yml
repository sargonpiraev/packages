# yaml-language-server: $schema=https://alint.org/_alint/configuration/schema.json
# Shared monorepo: product-project shape via @sargonpiraev/alint-config.
version: 1

extends:
  - ./packages/alint-config/project.yml
